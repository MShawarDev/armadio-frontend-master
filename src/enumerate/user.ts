export enum Roles {
  TRAINEE = 'trainee',
  CORPORATE = 'corporate planner',
  INSTRUCTOR = 'instructor',
}

export enum UserRoleType {
  trainee,
  corporateplanner,
  instructor,
}

export enum CoursesType {
  InClass = 1,
  OnlineTraining = 2,
  OnSiteTraining = 3,
  VirtualTraining = 4,
}
